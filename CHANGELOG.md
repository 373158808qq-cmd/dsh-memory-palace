# 更新日志（CHANGELOG）

> 本文件只记**能核对来源**的改动（开发台账 + 打包产物）。
> ★0.9.5 之前是内部迭代、没对外发过★，所以更早的条目只点到版本与主题，不编细节。
> 版本号与 `_dist/` 里的 tarball 一一对应。

## 0.10.3 — 2026-09-16 · 补擦：0.10.2 漏掉的私人注释

- 0.10.2 只删了**体检器退役清单数组**里那几条，**别处的注释里还留着**同一批字样
  （体检器的三处说明注释、语义判官的举例注释），CHANGELOG 自己也把作者本机路径写进去了 —— **全包复扫才发现**。
- 这一版把它们统一改成通用说法（"某个服务名 / 某个端口号 / STT 名词"这种），**仍然只动注释、不动逻辑**。
- 自证：**全包复扫，再无任何私人痕迹字样**（含 tarball 字节级检查）。
- ⚠️ **顺带记一条踩坑**：0.10.2 改完源码我**复用了同一个版本号**重打包 —— 结果 `dsh plugin add`
  **按版本判定"已装过"，根本没换新包**，门槛①当场报"3 个文件与源码不一致"。
  ★改了内容就必须升版本号★，别指望包管理器认出"同名不同内容"。

## 0.10.2 — 2026-09-16 · 入库打码（密钥不再跟着会话留下来）

- **新增「入库打码」**：会话转写**落盘之前**，把用户贴进聊天的密钥换成 `［已打码］` ——
  覆盖 GitHub PAT / `ghp_` 等五种、OpenAI 与 Anthropic 的 `sk-`、AWS `AKIA`、Slack `xox*`、以及**私钥块**。
  正文、标题、折叠摘要、**会话卡**四处都过一遍（这四处分别会进语料、索引、卡片和 Vault 检索面）。
  ★起因是真事★：用户把 GitHub token 贴进聊天，转写落盘后 token 明文躺在 `<记忆库>/corpus/*.md` 里。
  实测清理：真语料里 **6 份 / 84 处**旧密钥被换成占位符，之后**全库 0 份还能匹配到密钥形状**。
- **打码不是静默的**：`ingestCorpus()` 回报 `redacted` 计数，界面上能看到"打了几处"。
- 新增 `REDACT_VERSION`：**改了打码规则就 +1**，它进 `.ingest-state.json`；一变就把**已落盘的旧正文全部重转一遍**。
  （没有这个触发器，老文件里的密钥永远留在那儿 —— 就是"快照式索引必须有重建触发器"那条。）
- **体检器的出厂退役清单瘦身**：删掉 5 条**只对作者本机成立**的 needle（某个权重文件名 / 某个服务名 / 某个端口号 / 某个 STT 名词）。
  它们对使用者是"永远不命中的噪声"，而且会暴露"作者用过哪套 TTS"。现在只留**产品自身**的退役物三条。
  ⚠️ 代价如实写在这里：**作者本机也不再自动检测那几样旧物**了。

## 0.10.1 — 2026-09-16 · 发布前清理：源码注释里的个人路径

- **只动注释，不动任何逻辑**：7 个源文件里写着"从「作者本机路径 + `memory/tools/某工具.mjs`」原样搬进来"，
  那是**作者本机的工作区路径**，不该出现在公开仓库里 → 统一改成 `<工作区>/memory/tools/x.mjs`。
- 顺带把一处注释里夹着的具体路径换成通用说法（"工作区路径里可能带中文（如中文目录名）"）。
- 自证：改完复扫整个包，**源码里再无作者本机路径的字样**。

## 0.10.0 — 2026-09-16 · 首个对外发布版（许可与发布件齐了）

- **许可定为 Apache-2.0**：仓库根新增 `LICENSE`（apache.org 的**逐字节原文**，
  只把 APPENDIX 的 `[yyyy] [name of copyright owner]` 换成了 `Copyright 2026 373158808qq-cmd`）。
- 新增 `NOTICE`：写明本包许可，并**归属**随包分发的第三方组件
  `lib/vendor/dsh-persist/`（**MIT © 2026 ten**，原文仍在 `lib/vendor/dsh-persist/LICENSE`）。
  —— Apache-2.0 只在 Work 含 NOTICE 时才强制传递它，但收了一份 MIT 代码就该写清楚，属于该做。
- `package.json` 补齐发布元数据：`license` / `author` / `repository` / `homepage` / `bugs`，
  并把 `NOTICE` 加进 `files` 白名单（否则打包时会漏掉它）。
- **版本号停在 0.10.x，没有直接上 1.0.0**：这个包还没经过外部实测，1.0.0 是过度声明。
  朋友实测通过、确认"换台电脑也能装能用"之后再谈 1.0.0。
- `README` 的「许可」一节从 `TBD` 换成正式说明（含"改过的文件要注明改过"这条 Apache 特有条件）；
  安装示例里的 tarball 文件名同步到 0.10.0，并补一句"路径写绝对路径"。

## 0.9.10 — 2026-09-16 · 修：设置页「记忆系统」整块消失

- **修一个会把整个设置项弄没的 bug**：0.9.9 加"收起/展开"时，我把 `mExpand` 那个 `useState`
  写在了组件的**提前 return 之后**。React 要求每次渲染的 hook 顺序与数量完全一致 ——
  于是第二次渲染多出一个 hook → React 抛错 → **整个设置项被卸载**，表现为
  "设置里只剩「初始化」和「协作 SOP」，中间那块没了"。
  现在这个 state 挂在组件顶部（和别的维护 state 一起），并留了注释说明**不许再挪回去**。
- 夹具补上一道**能抓住这种 bug 的闸**：同一个组件连渲两次，**断言两次的 hook 数一致**
  （改坏之前 15 vs 16，现在 16 vs 16）。
- **修夹具自身的"假绿"**：那条断言原先被插在 `if (fail === 0) { … }` 的语句体里，
  结果**打印了 FAIL 却照样报"全过"、退出码 0**。现在判据与退出码只看 `fail`，插在哪儿都不影响真假。

## 0.9.9 — 2026-09-16 · 维护卡可收起

- 设置页「维护」卡**默认收起成一行**（状态摘要 + 根目录 + 【全部跑一遍】【展开▾】）——
  上一版把七个作业全铺开，太长。展开后仍是"一行一个作业"，逐行【看输出】，
  说明与"退出码是什么意思"只在输出里出现。
- 夹具补"默认收起不许回退"：不展开时**一行作业都不许渲染**。

## 0.9.8 — 2026-09-16 · 维护工具随包发布

- **7 件维护作业搬进插件** `lib/tools/`（体检器 8 项 / 裁决台账 / 分层纪律 / 注入预算 / 状态真话核对 /
  能力清单点名 / 备份记忆库）。此前它们只存在于开发者的工作区，**别人装了插件也没有**。
- 新增 `lib/tools/root.mjs`：根目录定位改成"**先问、后猜**"——
  `--root` > `~/.dsh-memory/init.json`（初始化时记下的）> 从当前目录往上找 > **如实说"不知道"**（绝不拿 cwd 凑合）。
- 新增 `lib/maintain.mjs` + `lib/maintain-cli.mjs`（`list` / `where` / `run <id|all>`）与两条路由
  `/dsh-memory-app/maintain/list`、`/maintain/run`。
- 设置页新增**「维护」卡**：七行作业、每行一个按钮，输出**原样展开**；每条都写明"退出码是什么意思"
  （非 0 不一定是坏了 —— 体检器/裁决台账是"有待你裁的条目"）。
- **修**：`budget` 在没有插件源码的机器上会崩（它去读 `<工作区>/plugins/…/index.js`）→ 改成明说"这几项跳过"。
- 包里不再混入退役的坏件（`client.js.disabled_broken_*` 移出发布内容）。

## 0.9.7 — 2026-09-16 · Obsidian 并入一键初始化

- 初始化任务书从"三问"扩到**四问**：根目录 → Ollama → 嵌入模型 → **Obsidian**。
  Obsidian 的**问与装在建记忆之前**（库要先登记好），**打开放在最后**（那时记忆视图已生成，树状图才是满的）。
- 新增 `lib/obsidian.mjs`：找 exe / 库登记 / 打开关闭的**唯一实现**（插件与 CLI 共用；其中"库登记"会写用户真实的 Obsidian 配置）。
- **修掉两处"假装成功"**：没装 Obsidian 时会退回 `obsidian://` 协议、命令一发出去就报成功，
  界面显示"已发出打开请求（若没弹出，看任务栏）"——其实什么都没发生。现在**如实报"没找到 Obsidian"**
  并给三条出路（winget 装 / 直接打开文件夹 / 用任何编辑器看 .md）；`registerError` 也不再被前端吞掉。

## 0.9.6 — 2026-09-16 · 一键初始化

- 把"建骨架 / 检测 Ollama / 嵌入模型"从**手工点按钮**并进**任务书**：点一下 → 开一个新会话 →
  它按顺序**问你三件事**（想用云端 API 就先停下，等你填好设置页再继续）。
- 新增 `lib/init-cli.mjs`（`status` / `detect` / `skeleton` / `ollama-locate` / `install-ollama` /
  `pull-model` / `use-api` / `view` / `mark`）：新会话里的 agent 用它干活，**每步回写 `~/.dsh-memory/init.json`**
  → 设置页那 5 行**自己变 ✓**。
- 设置页卡片改成「**1 键 + 5 行只读状态**」，原来的手动按钮折叠成「手动分步（备用）」。
- 新增 `lib/embedder-cfg.mjs`（嵌入器默认值的唯一实现）。
- **修**：`buildView` 在**还没有 vault.db 的新机器**上直接崩 → 导致卡片第 5 行永远变不绿。

## 0.9.5 — 2026-09-16 · 打通「进入新会话，配置」

- **根因**：客户端代码里读 `ctx.remote`，而 `remote` 不是 Cordis 服务 → 代理抛
  `cannot get property "remote" without inject`，异常又被 Promise 吞掉 → 点了**没有任何反应**。
- 改成**宿主侧建会话 + 投递**：`POST /dsh-memory-app/init/session`（用 DSH 自己的 `ctx.sessionController`），
  浏览器只负责把新会话切到前台；整条链加外层 `catch`，**每一步都写到卡片日志上**。
- 取服务一律走 `ctx.get()`（它只返回 undefined、不抛）。

## 0.9.0 – 0.9.4 — 2026-09-16（内部迭代）

- **「一个插件」**：把第三方 `dsh-persist` 的本地分叉收进包内、由 `ctx.plugin()` 挂载；
  客户端两半（设置卡 + 记忆 tab）合并。对外**只需要装一个插件**。
- 初始化向导初版：检测/安装 Ollama 与嵌入模型、选工作区建骨架、分析用户自己的文档生成专属 SOP、收尾体检。
- 出厂 SOP 只保留"目录结构"部分；新增 `init-task.md`（初始化任务书）。

## 0.6.4 – 0.8.x — 2026-09-14 ~ 09-16（打包封装之前）

- **0.6.8**：重建索引提速 —— 嵌入改走 `/api/embed` **批量 32**、入库改 `addMany` **单事务**、
  镜像只写一次；**872 块从 315.9 秒降到 20.1 秒（15.7×）**；顺带修掉">500 条整批返回空向量且不报错"的真 bug。
- **0.6.7**：修 `corpus-ingest` 的数码点 `RangeError`（71 MB 会话日志时会出现"正文写了、会话卡没建"）。
- **0.6.6**：记忆视图树状结构、`/tools` 路由、前端面指纹扩展到 host 入口。

---

## 版本号怎么走

`0.9.x` 是**对外可发布**的线；`0.6.x – 0.8.x` 是内部迭代。
每个发布版本都在开发仓库的 `_dist/` 留有对应 tarball，可回退。
